[project]
name = "fastmcp-google-oauth-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastmcp @ git+https://github.com/jlowin/fastmcp.git@main",
]
